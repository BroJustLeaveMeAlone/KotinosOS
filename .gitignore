# Build artifacts
build-output/
output/
*.vhd
*.vhdx
*.qcow2
*.raw
*.iso
*.vmdk

# Local overrides
.env
*.local.toml

# Development credentials overlay -- contains a password hash and SSH key
# baked into test images. Must never be committed.
dev-credentials.toml
config.dev.toml

# Test-VM keypair and credentials.
.dev-secrets/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini
